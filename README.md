# Charles Yi — Personal Site

This is a minimal, academic-style single-page site built with Next.js (App Router) and Tailwind CSS.

## Getting started

```bash
npm install
npm run dev
```

Then open `http://localhost:3000` in your browser.

